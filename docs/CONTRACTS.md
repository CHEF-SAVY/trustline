# CONTRACTS.md — source of truth

Placeholder. Filled in during **Phase 1** (function signatures + events) and extended in **Phase 2**
(the exact attestation byte layout the FCE emits).

Once populated this file — not the source — is what later phases build against. If an implementation
and this document disagree, that is a bug to resolve explicitly, not to paper over.

See `docs/PLAN.md` for the verified architecture this will document.
